[
  {
    "file": "src/parse/build/list.ts",
    "function": "buildList",
    "reason": "`items.at(-1) ?? first` re-checks the contract its own JSDoc states (\"never empty\" — the reader opens a list on a marker line and builds that item before closing it), and the fallback is not one: with no items `first` is undefined too, so the guard cannot make the function total. Designing it away means taking a non-empty item list, not choosing a better fallback."
  },
  {
    "file": "src/parse/build/paragraph.ts",
    "function": "buildLiteralParagraph",
    "reason": "`lines.at(-1) ?? first` re-checks that the reader's run of indented lines is non-empty, which is the only way readLiteralParagraph calls it. Same shape as buildList: an empty run leaves `first` undefined as well, so the fallback buys nothing it appears to."
  },
  {
    "file": "src/parse/lines/list-reader.ts",
    "function": "finish",
    "reason": "`last?.text` and `last !== undefined` inside `while (this.buffer.length > 0)` re-check exactly what the loop condition just established. `Array.prototype.at`'s `T | undefined` return is the whole reason the checks are written; nothing about the item extent permits the state."
  },
  {
    "file": "src/parse/inline/rules.ts",
    "function": "isBoundary",
    "reason": "`text.at(index) ?? \"\"` sits one line after the range guard that returns for every out-of-range index, so the miss cannot happen — and the fallback would answer WRONGLY if it did: `\"\"` is neither whitespace nor boundary punctuation, so it reports \"not a boundary\" where the guard above reports \"boundary\". A silent wrong answer is the cost of validating in the interior instead of taking an in-range index."
  }
]
