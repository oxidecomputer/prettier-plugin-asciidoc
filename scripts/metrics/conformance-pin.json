{
  "quarantined": 200,
  "note": "Cases tests/conformance/quarantine.json admits we format differently from the oracle. The manifest shrinks when a gap is fixed and grows when one is accepted; this pin makes both a deliberate, reviewable edit. Move it in the SAME commit that moves the manifest, and say which issue the move belongs to."
}
