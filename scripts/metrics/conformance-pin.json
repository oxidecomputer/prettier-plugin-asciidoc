{
  "quarantined": 160,
  "note": "Cases tests/conformance/quarantine.json admits we format differently from the oracle. The manifest shrinks when a gap is fixed and grows when one is accepted; this pin makes both a deliberate, reviewable edit. Move it in the SAME commit that moves the manifest, and say which issue the move belongs to. 171 -> 160 is issue #12: explicit ordered list markers are parsed, so the eleven cases whose ordered lists folded into a paragraph now agree with the oracle."
}
