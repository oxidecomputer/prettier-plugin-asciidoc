{
  "quarantined": 97,
  "note": "Cases tests/conformance/quarantine.json admits we format differently from the oracle. The manifest shrinks when a gap is fixed and grows when one is accepted; this pin makes both a deliberate, reviewable edit. Move it in the SAME commit that moves the manifest, and say which issue the move belongs to. 171 -> 160 is issue #12: explicit ordered list markers are parsed, so the eleven cases whose ordered lists folded into a paragraph now agree with the oracle. 160 -> 103 is issue #18 (the document header): 55 cases whose only divergence was the blank line the formatter inserted between the title and the author line, plus one #9 case and one untriaged case the same read fixed. 103 -> 97 is issue #24 (attribute entry line continuation): the six cases whose continued value became body text now agree with the oracle."
}
